import { RequestMetaValidationPipe } from './requestMetaValidation.pipe';

describe('RequestMetaValidationPipe', () => {
  it('should be defined', () => {
    expect(new RequestMetaValidationPipe()).toBeDefined();
  });
});
